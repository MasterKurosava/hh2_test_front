export const OutputContainer: React.FC = () => {
    return (
        <div className={`flex items-center justify-center bg-black text-white w-full h-full text-center col-span-1`}>
        Контейнер
        </div>
    );
};

export default OutputContainer;